/* hello_world.c
   DD1324 
   this is a multi-line comment */
#include <stdio.h>  // Includes a standard library which makes us be able to use printf
 	
int main()  // Initialize the main function, crucial for c-programs
{ // Sets bounderies of the main function
  printf("hello world!\n");  // this is a single line comment, it prints it to the console
  return 0;  // This returns a 0 when program is finished. 0 signifies that everything went well
}
