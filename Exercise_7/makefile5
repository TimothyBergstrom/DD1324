# Variables
# Make everything depend on headers, so if headers change, it will recompile everything
SOURCE		= exercise7-5.c
TARGET 		= exercise7-5
CC			= gcc
CFLAGS		= -Wall -g

program: exercise7-5.c
	$(CC) $(CFLAGS) $(SOURCE) -o $(TARGET)

# clean all
clean:
	-rm $(TARGET)


