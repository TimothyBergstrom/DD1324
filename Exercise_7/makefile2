# Variables
# Make everything depend on headers, so if headers change, it will recompile everything
SOURCE		= exercise7-2.c
TARGET 		= exercise7-2
CC			= gcc
CFLAGS		= -Wall -g

program: exercise7-2.c
	$(CC) $(CFLAGS) $(SOURCE) -o $(TARGET)

# clean all
clean:
	-rm $(TARGET)


